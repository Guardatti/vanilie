{
    "buildCommand": "cd vanilie && npm install && npm run build",
    "outputDirectory": "vanilie/dist",
    "installCommand": "cd backend && npm install",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/backend/app.ts"
        }
    ]
}