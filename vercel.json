{
    "buildCommand": "cd vanille && npm install && npm run build",
    "outputDirectory": "vanille/dist",
    "installCommand": "cd backend && npm install",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/backend/app.ts"
        }
    ]
}